{
  "scripts":{
    "dev": "yarn data && npx vuepress dev",
    "build": "yarn data && npx vuepress build",
    "data":"node convert_excel_json.js"
  },
  "devDependencies": {
    "vuepress": "^1.0.0-rc.1",
    "xlsx": "^0.14.3"
  }
}
